// Optional: You can add minimal JavaScript here if needed,
// e.g., for very basic client-side form validation or UI enhancements.

// The contact form submission handling is already inlined in contact.html
// as per the requirements for a purely static site without backend processing.

// Example of simple FAQ accordion (if you decide to implement FAQ answer toggling):
/*
document.addEventListener('DOMContentLoaded', function() {
    const faqItems = document.querySelectorAll('.faq-item');

    faqItems.forEach(item => {
        const question = item.querySelector('h3');
        const answer = item.querySelector('p');

        question.addEventListener('click', function() {
            answer.style.display = answer.style.display === 'none' ? 'block' : 'none';
        });
    });
});
*/